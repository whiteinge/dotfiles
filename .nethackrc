# Harrigan's nethackrc
# $Id$
# for nethack 3.4.3 with the menucolors patch

# Set default names for common game items & egg laying (woot!)
OPTIONS=name:Harrigan,gender:female
OPTIONS=fruit:baby,dogname:Snoopy,catname:Puss,horsename:Silver

# Graphics
OPTIONS=windowtype=TTY,!silent
OPTIONS=color,lit_corridor,standout,use_inverse,hilite_pet,boulder=0
OPTIONS=time,showborn,showexp,showscore,perm_invent

# Menus
OPTIONS=menustyle:full,menu_headings:bold
OPTIONS=sortpack,lootabc,fixinv

# Messages: ctrl-p to view old messages
OPTIONS=msg_window=r,msghistory=30

# Controls
OPTIONS=number_pad=0
OPTIONS=runmode:walk

# Safe Options
OPTIONS=safe_pet,pickup_burden:Burdened
OPTIONS=!autopickup,pickup_types:$

#
# MenuColors is a 3rd-party patch, ymmv
#
OPTIONS=menucolors
MENUCOLOR="uncursed"=green
MENUCOLOR="blessed"=cyan&bold
MENUCOLOR="cursed"=orange&bold
MENUCOLOR="loadstone\|wand \(of\|called\) cancellation"=underline
# color only the real Amulet
MENUCOLOR="Amulet of Yendor named"=magenta
MENUCOLOR="gold piece"=yellow
# forgotten spell
MENUCOLOR="[a-zA-Z] - [a-zA-Z ]+ [0-9]\*   [a-z]+ +[0-9]+%"=magenta
