align -- Align columns at CHAR
alphaindex -- Get unique alphabet character(s) from a positive integer
ansi-strip -- Remove ansi terminal formatting codes
bookletize -- Convert a PDF to a fold-able booklet sized PDF
bootstrap-fedora -- Bootstrap a default Fedora install
bootstrap-osx -- Bootstrap a new OS X machine
bootstrap-pocket2 -- Bootstrap a new Pocket 2 machine
bootstrap-wsl -- Bootstrap a WSL Ubuntu install
bootstrap-yt-dlp -- Bootstrap a yt-dlp install
busywork -- Silly functions to make a terminal look busy like in a movie
caffeine -- Prevent the screensaver from activating or the screen from sleeping
chop -- Chop lines to available terminal width
click -- Use a fuzzy-finder to open URLs found in the scrollback buffer
clip -- A wrapper around xclip to make using the clipboard faster
cloc-git -- Temporarily clone a repository and run cloc on it
clot -- Fill screen with garbage as visual separator
colors-waterfall -- Output the spectrum and each number for 256 palette
connected_ips.awk -- Output IP addresses and ports for machines connected to the local machine
control-master -- View and manage SSH ControlMaster socket connections in bulk
count -- Count occurrences of the same line in a file
countdown -- A simple countdown timer
csv2table -- Output csv fields as a markdown table.
ctags-to-quickfix -- Format the output from readtags to Vim's quickfix format
cull-brew -- Remove any brew packages that are not on a whitelist
cull-dnf -- Remove any user-installed packages not on a whitelist
c -- Wrap curl to conditionally open headers, timing info, and response in a pager
devdocs-local -- Select docs from devdocs.io using a fuzzy-finder for display at the CLI
diffconflicts -- A better vimdiff mergetool for Git
diff-to-line-numbers -- Output the line numbers for lines added and removed in a unified diff
diff-to-quickfix -- Convert a diff into Vim's quickfix format
echoc -- Print each input character on a new line
echol -- Like echo but delimit with newlines instead of spaces
echos -- Like echo but using printf for safety
explode_path -- Take a path and output all child paths as absolute paths
fbin -- Fuzzy-find scripts in $HOME/bin by description
fdlna -- Browse through dlna media directories using a fuzzy-finder
femoji -- Add simple emoji fuzzy-finder
ffmpeg-audible -- Convert an aax file to an m4b file
ffmpeg-blur -- Blur a section of video based on an image mask
ffmpeg-clip -- Extract a clip from a larger video file
ffmpeg-genchapters -- Generate chapters meta data from a directory of audio files
ffmpeg-m4b -- Concat or convert audio files into a single m4b with chapters and thumbnail
ffmpeg-screencast -- Record a screencast on X11 using ffmpeg
ffmpeg-segments -- Extract a long video into same-length video segments
field -- Output a name or numbered field from a delimited input
flipbook -- A flipbook loop for ascii drawings
flr -- (filtering lr) - A wrapper around lr to ignore noise directories
fnode -- Use a fuzzy-finder to select and install a NodeJS version
ftimezone -- Fuzzy-find available timezones
ftree -- Use a fuzzy-finder to traverse the file system and print a file name
_fzy_cd -- Use a fuzzy-finder to complete child directories
_fzy_git -- Use a fuzzy-finder to complete Git refs
_fzy_gpg -- Use a fuzzy-finder to select a GPG key
_fzy_HandBrakeCLI -- Use a fuzzy-finder to select a HandBrake preset
_fzy_iconv -- Use a fuzzy-finder to complete encodings for iconv
_fzy_kill -- Use a fuzzy-finder to complete process names
_fzy_man -- Use a fuzzy-finder to complete manpage names
_fzy_mycli -- Use a fuzzy-finder for mycli
_fzy_npm -- Use a fuzzy-finder for npm scripts
_fzy_pandoc -- Use a fuzzy-finder to select Pandoc input/output formats
_fzy_podman -- Use a fuzzy-finder to complete Podman/Docker container IDs
_fzy_ssh -- Use a fuzzy-finder for hostnames in ~/.ssh/config
genmac -- Generate a locally-assigned (starts with 02) mac address from a salt
genpass -- Generate a cryptographically inadequate(!) password
genreadme -- Generate a README for all ~/bin scripts
ghkeys -- Display GitHub public keys for a user
git-attic -- List deleted files in a Git repository
git-counter -- Atomically increment a counter value stored in a Git repository
git-fetchall -- Run `git fetch` on all repos under the current dir in parallel
git-graph-dag -- Draw a graphviz diagram of the Git DAG
git-pagediffs -- Paginate individual Git commits as separate files in $PAGER
git-prompt -- Output a fast, succinct, one-line Git status for use in a prompt
git-quickfix -- Open Vim with Git changes as quickfix entries
git-sizes -- Output the largest objects in a repository
git-stub -- Stub Git commits (touch a file, stage the file, commit the file) for demoing
git-summary -- Show a summary of the commits, authors, and files in a Git repo
git-todo -- Store a todo list in any Git repository
git-web-uri -- Generate a GitHub/GitLab permalink to the given file/line
glr -- A wrapper to parallelize grep and optionally filter by filename pattern
hr -- Print a line of characters
htmlstar -- Use xmlstarlet with html
httpdir -- Serve a local directory via HTTP (via socat) with or without SSL
httptest -- A local HTTP server with many endpoints to test HTTP request/responses
imenu -- Select multiple items from stdin via abc's like the inventory menu in Nethack
invoice -- Generate a PDF invoice from a plain text time log using groff and m4
joinpdf -- Merges, or joins multiple PDF files into "joined.pdf"
join-strings -- Join strings together with a delimiter
jqflat -- Use jq to flatten a JSON structure for easy use with awk/grep/etc
jqjsonl -- Output JSONL from an array- or object-of-objects
linux-resize-window -- Select a window in X11 and resize it to given dimensions
lndir.sh -- Create a shadow directory of symbolic links to another directory tree
lorem -- Output paragraphs of lorem ipsum placeholder text
_lr -- Wrap lr to use standard colors and improve usability as ls replacement
luksimg -- Wrappers around cryptsetup to create/mount/unmount encrypted images
mailto-handler -- A mailto: handler for opening an arbitrary webmail link in Firefox
match-emails -- Output all matched email addresses
match-git-range -- Output all matched Git SHA ranges (e.g., 123456..654321)
match-ips -- Output all matched IP addresses
match-urls -- Output all matched URLs
mdheader -- Output the first line as a Markdown header, then other lines verbatim
mdless -- View rendered Markdown files with $MANPAGER
new-sh -- Quickly create and edit a new shell script, already marked as executable
nightmode -- Calculate a color value for the current time to gradually red-shift via cron
node_complete.pl -- Configure a NodeJS repl with both readline and completion
noderepl.js -- Configure a NodeJS repl with both readline and completion
npm-deps-report -- Output how many sub-dependencies each npm dependencies has
osx-keyboard-linux -- Make an OS X keyboard work like a PC keyboard
osx-notify-send -- A notify-send-like wrapper to OSX's notify applescript thingie
osx-proxy -- Toggle the OSX HTTP/HTTPS proxies on for the primary interface
osx-resize-window -- Resize the most recent window in a specific app to specific dimensions
osx-toggle-interfaces -- Toggle wifi/ethernet on/off when the physical connection changes
osx-xclip -- An xclip-like wrapper to pbcopy/pbpaste
play-somafm -- Select a SomaFM stream with a fuzy-finder and play via mpv
pysmtp -- Start a echoing SMTP server
queue -- A lightweight job queue using tail and xe
replace-link -- Replace a symlink with a copy of the thing it points to
requote -- Replace "fancy" quotes with ASCII quotes
res -- Output an xrandr command for common configurations
rgb2hex -- Convert 16-bit RGB values to hexadecimal
rproxy -- A reverse proxy that pipes the HTTP response through a shell command
rtimemachine -- Use rsync as a drop-in replacement for OSX Time Machine
scrollback -- Output the tmux scrollback for the current pane in reverse order
shsplit -- Reformat a one-line shell command as a multiline shell command
slugify -- Output a string as a slug
split-strings -- Split a string on a common delimiter
status -- Collect various system stats for feeding into a status bar
steam-disable-origin-overlay -- Disable the EA Origin overlay for Steam games
tailsplit -- Run a command and open two tmux panes to tail stdout and stderr separately
tclip -- Read/write tmux buffer contents via a fuzzy-finder
thes -- Search Grady Ward's Moby Project thesaurus with a fuzzy-finder
timebuddy -- Compare columns of hours in a day across multiple timezones
timer -- Output seconds elapsed
tmux-tiled -- Make a bunch of tiled tmux panes via find -exec or xargs
total-running -- Show a running total of seen lines
total -- Sum (total) numbers in a column
transpose -- Transpose rows of key/value pairs to aligned key/value columns
unique-lines -- Remove duplicate lines (no sort requirement)
urldecode -- Decode urlencoded stdin
urlencode -- Urlencode stdin
vipe -- Use $EDITOR to edit text inside a pipeline
watch-changes -- A simple polling file/directory watcher
watch-net -- Monitor current bandwidth usage by watching /proc/net/dev
which-open -- Run the correct 'open' command for the current platform
wikipedia -- Fetch the Wikipedia summary paragraph for a search query
wsl-notify-send -- A notify-send-like wrapper for WSL
wsl-xclip -- An xclip-like wrapper for wsl
xfzy -- Open fzy in a new, temporary xterm window
zombies -- List all zombie processes and their parents (to kill)
zshrun -- Invoke a Terminal.app window and block until it closes
