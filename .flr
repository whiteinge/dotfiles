# VCS dirs
path ~~ "*.bzr"
path ~~ "*.git"
path ~~ "*.hg"
path ~~ "*.svn"

# Common JS dirs
path ~~ "*_build"
path ~~ "*build"
path ~~ "*dist"
path ~~ "*node_modules"
path ~~ "*site-packages"
path ~~ "*dist-packages"

# Common JS build files
name ~~ "*.bundle.js"
name ~~ "*.min.css"
name ~~ "*.min.js"
name ~~ "package-lock.json"

# Python build files
name ~~ "*.pyc"
name ~~ "*.pyo"

# ctags
name == "tags"

# Vim plugins
path ~~ "*.vim/pack"
